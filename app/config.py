"""
Project configuration: status codes, metric definitions, benchmark rates.

CRM Status codes:
    0  => Новый
    1  => Обработан
    2  => Отправлен
    3  => Доставлен
    4  => Вручен
    5  => Возврат
    6  => Отменён
    7  => Удалён
    8  => Оплачен
    9  => Недозвон
    10 => Перезвон
    12 => Вруч-Возврат
    13 => Контрольный прозвон
    14 => Ожидает отправки
    17 => Отказ
    18 => Треш
    24 => Отказ (нестандартный)
    27 => Ожидает поставки
    29 => Перезвон
"""

# Статусы, считающиеся апрувом (подтверждён колл-центром)
APPROVE_STATUSES: set[int] = {2, 3, 4, 5, 8, 12, 13, 14}

# Статусы, считающиеся выкупом (клиент забрал на почте / оплатил)
BUYOUT_STATUSES: set[int] = {3, 4, 8}

# Статусы треша (невалидные лиды)
TRASH_STATUSES: set[int] = {18}

# Статусы отказа (клиент отказался, не дозвон и т.п.) — включая 24
REJECT_STATUSES: set[int] = {6, 9, 10, 17, 24, 29}

# Целевые (бенчмарк) ставки выкупа по возрасту лида в рабочих днях.
# Ключ = кол-во дней с момента создания лида (1 = вчера, 8 = 8 дней назад).
# Лид «старше» 8 дней использует значение для ключа 8.
BUYOUT_BENCHMARK: dict[int, float] = {
    1: 0.05,
    2: 0.10,
    3: 0.15,
    4: 0.25,
    5: 0.35,
    6: 0.45,
    7: 0.55,
    8: 0.65,
}

# Окно для 8-дневного скора (количество рабочих дней)
SCORING_WINDOW_DAYS: int = 8
